using UnityEngine;
using System.Collections;

public class TestScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
        Debug.Log("Hello world from VS!");
    }
	
	// Update is called once per frame
	void Update () {
	
	}
}
